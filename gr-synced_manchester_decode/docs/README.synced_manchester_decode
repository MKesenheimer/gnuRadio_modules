This is the synced_manchester_decode-write-a-block package meant as a guide to building
out-of-tree packages. To use the synced_manchester_decode blocks, the Python namespaces
is in 'synced_manchester_decode', which is imported as:

    import synced_manchester_decode

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(synced_manchester_decode)
